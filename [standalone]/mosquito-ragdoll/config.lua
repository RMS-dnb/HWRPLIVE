Config = {}

-- List of controls and hashes for them: https://github.com/femga/rdr3_discoveries/blob/master/Controls/README.md
Config.ToggleRagdoll = 'INPUT_FRONTEND_RT' -- Z is Default but you can change the key to whatever you want.

Config.Optimize = 500 -- Checking if Ped is in melee every 500ms, the lower the value the more accurate but more performance heavy